# Не тащим в build-контекст то, что ставится/собирается внутри образа или не нужно.
# Важно: node_modules исключены, иначе локальные pnpm-симлинки (абсолютные пути)
# затрут установленные в стадии deps и сломают резолв зависимостей в контейнере.
**/node_modules
**/dist
.git
.github

# Локальные данные/секреты — не должны попасть в образ.
.env
*.env
!.env.example
data
backups
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Прочее
.vscode
.idea
*.log
