---
# Раскатка DankoDeploy на VPS: Docker → код → Traefik (общий edge) → секреты → запуск.
# Независимо от состояния сервера: ставит Docker, сеть web и Traefik, если их ещё нет.
- import_tasks: docker.yml
  tags: [docker]

- import_tasks: code.yml
  tags: [code]

- import_tasks: traefik.yml
  tags: [traefik]

- import_tasks: secrets.yml
  tags: [secrets]

- import_tasks: deploy.yml
  tags: [deploy]
